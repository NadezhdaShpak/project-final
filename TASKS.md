Сбилдить приложение: mvn clean install
docker run -p 5432:5432 --name postgres-db -e POSTGRES_USER=jira -e POSTGRES_PASSWORD=JiraRush -e POSTGRES_DB=jira -e PGDATA=/var/lib/postgresql/data/pgdata -v ./pgdata:/var/lib/postgresql/data -d postgres
Запуск приложения с профилем прод
java -jar target/jira-1.0.jar --spring.profiles.active=prod
admin@gmail.com 

Список выполненных задач:
1. Разобраться со структурой проекта (onboarding).
   - в папке bugtracking в подпапках находяттся наборы модель, сервис, репозиторий, маппер и ТО, контроллер</br>
   - common классы ошибок, конфиги</br>
   - login - авторизация пользователей
2. Удалить социальные сети: vk, yandex. </br>
   - Удалены все упоминания данных соц сетей
3. Вынести чувствительную информацию в отдельный проперти файл</br>
Значения этих проперти **считываются** при старте сервера **из переменных окружения машины**.</br>
$\hspace{10pt}$[x] логин</br>
$\hspace{10pt}$[x] пароль БД </br>
$\hspace{10pt}$[x] идентификаторы для OAuth регистрации/авторизации</br>
$\hspace{10pt}$[x] настройки почты</br>
   
4. Переделать тесты так, чтоб во время тестов использовалась in memory БД (H2), а не PostgreSQL. Для этого нужно определить 2 бина, и выборка какой из них использовать должно определяться активным профилем Spring. H2 не поддерживает все фичи, которые есть у PostgreSQL, поэтому тебе придется немного упростить скрипты с тестовыми данными.
   - Добавлен [changelog-test.sql](src/test/resources/changelog-test.sql) адаптированый для работы с H2;
   - Добавлен [DataSourceConfig.java](src/main/java/com/javarush/jira/config/DataSourceConfig.java), в котором определяется активный профиль. Для тестов с использованием H2 выбранEmbeddedDatabaseBuilder для простого заполнение данных в тестах.
   - В [application-test.yaml](src/test/resources/application-test.yaml) изменены настройки для работы с H2;
   - В сущности Contact поле value переименовано в val (является зарезервированым словом в H2) 
   - Так как H2 строже соблюдает уникальные индексы, чем PostgreSQL, пришлось поправить вставку в user_belong для прохождения теста
   - 142 tests passed
5. Написать тесты для всех публичных методов контроллера [ProfileRestController.java](src/main/java/com/javarush/jira/profile/internal/web/ProfileRestController.java). Хоть методов только 2, но тестовых методов должно быть больше, т.к. нужно проверить success and unsuccess path.
созданы тесты для:
   - вход авторизированого пользователя и гостя
   - вход не авторизированного пользователя
   - обновление профиля
   - проверка обновления не валидного профиля
   - проверка отработки аннотации @NoHtml у значения Контактов при создании профиля HTML-инъекций
   - проверка обновления с некорректными типами уведомлений
   - проверка обновления с некорректными кодом контакта
6. Сделать рефакторинг метода com.javarush.jira.bugtracking.attachment.FileUtil#upload чтоб он использовал современный подход для работы с файловой системмой. </br>
   - Для создания файла вместо использования устаревшего класса File, методы которого, например, не кидают исключения с детальной информацией, на более современный класс Files.
   - Так как нету информации на сколько большие файлы будут использованы, было принято решение использовать Files.copy() с InputStream (для возможности загрузки больших файлов c перезаписью файла, если такой уже существует.
7. Добавить новый функционал: добавления тегов к задаче (REST API + реализация на сервисе). Фронт делать необязательно. Таблица task_tag уже создана.
8. Добавить подсчет времени сколько задача находилась в работе и тестировании. Написать 2 метода на уровне сервиса, которые параметром принимают задачу и возвращают затраченное время:
   </br>Сколько задача находилась в работе (ready_for_review минус in_progress ).
   </br>Сколько задача находилась на тестировании (done минус ready_for_review).
   </br> Для написания этого задания, нужно добавить в конец скрипта инициализации базы данных changelog-test.sql 3 записи в таблицу ACTIVITY
   </br>
   </br> insert into ACTIVITY ( ID, AUTHOR_ID, TASK_ID, UPDATED, STATUS_CODE ) values ...
   </br> Со статусами:
   </br>время начала работы над задачей – in_progress
   </br> время окончания разработки - ready_for_review
   </br>время конца тестирования - done
9. Написать Dockerfile для основного сервера
10. Написать docker-compose файл для запуска контейнера сервера вместе с БД и nginx. Для nginx используй конфиг-файл config/nginx.conf. При необходимости файл конфига можно редактировать. Hard task
11. Добавить локализацию минимум на двух языках для шаблонов писем (mails) и стартовой страницы index.html.
- В классе MvcConfig добавлены бины для работы с локалями, выбраная локаль хранится в куках браузера.
- Сообщения читаются из файлов messages.properties
- Добавлена поддержка для шаблонов писем и стартовой страницы index.html английского, испанского и русского языков
12. Переделать механизм распознавания «свой-чужой» между фронтом и беком с JSESSIONID на JWT. Из сложностей – тебе придётся переделать отправку форм с фронта, чтоб добавлять хедер аутентификации. Extra-hard task